
import 'package:flutter/material.dart';

class BadView extends StatelessWidget {
  const BadView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Something went wrong");
  }
  
}